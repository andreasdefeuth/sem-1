# Install

```shell
npm install pg-upload
```

# API

```ts
upload(db: pg.Client, file: string, sql: string): Promise<void>
```

# Use

Upload a local CSV-file to a database server for import
using the PostgreSQL copy command:

```js
import pg from 'pg';
import upload from 'pg-upload';

const db = new pg.Client({...});
await db.connect(); 
await upload(db, 'some/path/to/data.csv', `
    copy target_table (col1, col2, col3)
    from stdin
    with csv header encoding 'UTF-8'
`);
await db.end();
```